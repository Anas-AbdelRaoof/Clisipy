# requirements.txt — pinned latest stable versions (generated 2026-07-05)
# Notes:
# - Removed standard-library entries (sys, os). They should not be listed in requirements.
# - Pinned to latest released versions available on PyPI.

python-dotenv==1.2.2
groq==1.5.0
rich==15.0.0
